//// 7. Split a string into an array of substrings

function splitString(str, delimiter) {
  return str.split(delimiter);
}

// Example:
console.log(splitString("hello world", " ")); // Output: ["hello", "world"]
